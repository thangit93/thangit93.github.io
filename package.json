{
  "name": "resume-nuxt",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "gh-pages -d dist",
    "deploy2": "push-dir --dir=dist --branch=master"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "nuxt": "^3.13.0",
    "push-dir": "^0.4.1"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  }
}
